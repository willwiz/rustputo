# This file is autogenerated by maturin v1.8.7
# To update, run
#
#    maturin generate-ci github
#
name: CI

on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
            target: x86_64
        python-version: ["3.13", "3.12"]
    name: Build on Linux ${{ matrix.platform.target }} with Python ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - name: Test Rust Models Library
        working-directory: ./rust/models
        run: |
          cargo test
      - name: Testing Build from Source
        run: |
          pip install .
      - name: Run tests
        run: |
          pip install pytest
          pytest tests/